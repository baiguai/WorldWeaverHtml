{object, key=xplt_cloak, name=The Cloak
    {attrib, output=false, tags=invisible }
    {attrib, output=3, tags=hidden_rounds }
    {attrib, output=0, tags=cur_hidden_round }

    {input, syntax=[tplt_syntax]((use_cloak))
        {set, logic=[self]((invisible)), output='true' }

        {message
            output={@
You activate the Cloak Exploit.

            }
        }
    }
}
