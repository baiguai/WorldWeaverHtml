{x_stove
    {message
        output={@

You examine the stove...

        }
    }
}

{x_on_stove
    {message
        output={@

You look on the stove...

        }
    }
}

{x_under_stove
    {message
        output={@

You look under the stove...

        }
    }
}

{x_behind_stove
    {message
        output={@

You look behind the stove...

        }
    }
}













{input, syntax=[tplt_syntax]((x_stove))
    {act, logic=x_stove, tags=!_type|room }
}

{input, syntax=[tplt_syntax]((x_on_stove))
    {act, logic=x_on_stove, tags=!_type|room }
}

{input, syntax=[tplt_syntax]((x_under_stove))
    {act, logic=x_under_stove, tags=!_type|room }
}

{input, syntax=[tplt_syntax]((x_behind_stove))
    {act, logic=x_behind_stove, tags=!_type|room }
}
